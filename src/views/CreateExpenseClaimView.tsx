import * as React from 'react';
import {Component} from 'react';

interface CreateExpenseClaimViewProps {
  // prop1: number;
  // prop2: string;
}

interface CreateExpenseClaimViewState {
  // stateItem1: boolean;
}

export class CreateExpenseClaimView extends Component<CreateExpenseClaimViewProps, CreateExpenseClaimViewState> {
  // constructor(props: ClaimsViewProps) {
  //   super(props);
  //   this.state = {
  //     stateItem1: false
  //   };
  // }

  render() {
    return (
      <div>
        CreateExpenseClaimView
      </div>
    );
  }
}